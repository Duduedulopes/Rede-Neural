# 29/08/2026 — O esqueleto

Estrutura criada. Nenhuma conta implementada ainda, de proposito: primeiro os
contratos, depois a matematica.

## Decisoes

**Nielsen so como referencia.** O codigo dele e Python 2.7 (`xrange`,
`print "..."`). Nao roda aqui. Fica ao lado para consultar; o que roda e o
nosso, escrito por mim em Python 3.

**Com classe `Camada`.** O livro guarda listas de matrizes na rede. Preferi a
classe porque "camada de entrada, ocultas, saida" e o conceito em estudo, e
conceito que aparece no codigo se aprende melhor.
Custo assumido: `camadas[2].pesos` daqui e `weights[1]` la.

**Retropropagacao em arquivo proprio.** Contraria o livro, que faz dela um
metodo. Ela e o amago do projeto; nao fica escondida entre outros metodos.

**Nada de pasta para os tokens ainda.** Entram quando forem desenhados.

## O truque do esqueleto

Todo corpo levanta `NotImplementedError("Fase 1")`. O teste
`test_ainda_nao_implementado` cobra cada um. Quando a lista esvazia, a fase
acabou. Melhor que TODO em comentario: o pytest nao deixa esquecer.

## Estado

7 testes passando.

## Proximo

Sigmoid, matrizes, gradiente, entropia cruzada. Comecando pelo XOR.
